import homepage from "./homepage";
import service from "./services";
import projects from "./projects";
import innerPageContent from "./innerPageContent";

export const schemaTypes = [homepage, service, projects, innerPageContent];
